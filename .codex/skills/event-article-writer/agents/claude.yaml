interface:
  display_name: "Event Article Writer"
  short_description: "Create and update event articles for the Akiba site"
  default_prompt: "Use $event-article-writer to add or refresh an Akiba event article from source pages. Prefer the bundled harvester detail/dedup helpers for source fetching and duplicate checks when network or DNS conditions are flaky."
